name: Build Multi-Platform

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  actions: write
  pull-requests: write
  issues: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
            target: "aarch64-apple-darwin"
            arch: "arm64"
            platform_name: "macos-arm64"
          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
            target: "x86_64-apple-darwin"
            arch: "x64"
            platform_name: "macos-x64"
          - platform: "ubuntu-22.04"
            args: ""
            arch: "x64"
            platform_name: "linux-amd64"
          - platform: "windows-latest"
            args: ""
            arch: "x64"
            platform_name: "windows-x64"

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Rust setup (with target)
        if: matrix.target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Rust setup (default)
        if: ${{ !matrix.target }}
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Update npm to latest
        run: npm install -g npm@latest

      - name: Clean and install frontend dependencies (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          if (Test-Path node_modules) { Remove-Item -Recurse -Force node_modules }
          if (Test-Path package-lock.json) { Remove-Item -Force package-lock.json }
          npm install

      - name: Clean and install frontend dependencies (Unix)
        if: matrix.platform != 'windows-latest'
        run: |
          rm -rf node_modules package-lock.json
          npm install

      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "ZAugment ${{ github.ref_name }}"
          releaseBody: |
            ## 🚀 ZAugment ${{ github.ref_name }} 发布

            ### 更新内容
            1. ✅ 优化 Session 格式验证：从 `.eJx` 改为首位是 `.` 且长度大于 60 位，支持更多 Session 格式
            2. ✅ 禁用全局 F 类快捷键：禁用 F1-F12 功能键，防止误触系统快捷键（如 F5 刷新、F12 开发者工具等）
            3. ✅ 优化开关交互：设置页面的三个开关（导入导出时创建标签、自动上传、自动下载）现在只能点击开关本身切换，避免误触
            4. ✅ 修复 Session 批量导入保存失败问题：增加重试机制和错误处理
            5. ✅ 性能优化：Session 批量导入改为并行处理，速度大幅提升
            6. ✅ 修复 Session 复制功能：通过 Session 导入的账号现在可以正常复制 Session

            ### 📥 下载文件
            | 平台 | 文件名 |
            |------|--------|
            | **Windows** | `ZAugment_${{ github.ref_name }}_x64-setup.exe` |
            | **macOS (Intel)** | `ZAugment_${{ github.ref_name }}_x64.dmg` |
            | **macOS (Apple Silicon)** | `ZAugment_${{ github.ref_name }}_aarch64.dmg` |
            | **Linux (Ubuntu/Debian)** | `ZAugment_${{ github.ref_name }}_amd64.deb` |
            | **Linux (CentOS/RHEL)** | `ZAugment-${{ github.ref_name }}-1.x86_64.rpm` |

            ### 🔧 安装方法
            **Windows:** 下载 `.exe` 文件双击安装，如遇警告点击"更多信息" → "仍要运行"

            **macOS:** 下载对应芯片的 `.dmg` 文件拖拽到应用程序文件夹，如遇安装包已损坏运行：
            sudo xattr -rd com.apple.quarantine /Applications/ZAugment.app

            **Linux:**
             Ubuntu/Debian
            sudo dpkg -i ZAugment_${{ github.ref_name }}_amd64.deb
             CentOS/RHEL/Fedora
            sudo rpm -i ZAugment-${{ github.ref_name }}-1.x86_64.rpm


            ---
            **🔒 安全说明**: 如遇安全软件误报属正常现象
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
          includeUpdaterJson: false

      - name: Upload artifacts (macOS)
        if: matrix.platform == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-${{ matrix.arch }}-build
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*
            !src-tauri/target/${{ matrix.target }}/release/bundle/**/.*
          retention-days: 30
          if-no-files-found: warn

      - name: Upload artifacts (Windows)
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-${{ matrix.arch }}-build
          path: |
            src-tauri/target/release/bundle/**/*
            !src-tauri/target/release/bundle/**/.*
          retention-days: 30
          if-no-files-found: warn

      - name: Upload artifacts (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-${{ matrix.arch }}-build
          path: |
            src-tauri/target/release/bundle/**/*
            !src-tauri/target/release/bundle/**/.*
          retention-days: 30
          if-no-files-found: warn
